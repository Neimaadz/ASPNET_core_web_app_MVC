@model ASPNET_core_web_app_MVC.Models.UserCredential

@{
    @*Layout = null;*@
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login</title>
</head>
<body>
    @using (Html.BeginForm("Authentication", "Home", FormMethod.Post))    // Send to "ActionLogIn" of "HomeController" in method Post
    {
        <table cellpadding="0" cellspacing="0">
            <tr>
                <th colspan="2" align="center">Login</th>
            </tr>
            <tr>
                <td>Username: </td>
                <td>
                    @Html.TextBoxFor(m => m.Username, new { type = "text", required = "required" })
                </td>
            </tr>
            <tr>
                <td>Password: </td>
                <td>
                    @Html.PasswordFor(m => m.Password, new { type = "password", required = "required" })
                </td>
            </tr>
            <tr>
                <td></td>
                <td><input type="submit" value="Log in" /></td>
            </tr>
        </table>
    }
</body>
</html>