@model ASPNET_core_web_app_MVC.Models.UserCredential

@{
    @*Layout = null;*@
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Signin</title>
</head>
<body>
    @using (Html.BeginForm("Signin", "Home", FormMethod.Post))    // Send to "ActionSignIn" of "HomeController" in method Post
    {
        <div class="form-row">
            <div class="col-md-4 mb-3 mx-auto">
                <h3 class="text-center my-3">SIGNIN</h3>
            </div>
        </div>

        <div class="form-row">
            <div class="col-md-4 mb-3 mx-auto">
                <label for="username">Username:</label>
                @Html.TextBoxFor(m => m.Username, new { required = "required", @class = "form-control" })
            </div>
        </div>
        <div class="form-row">
            <div class="col-md-4 mb-3 mx-auto">
                <label for="password">Password:</label>
                @Html.PasswordFor(m => m.Password, new { required = "required", @class = "form-control" })
            </div>
        </div>
        <div class="form-row">
            <button class="btn btn-primary mx-auto" type="submit">Sign In</button>
        </div>
    }
</body>
</html>